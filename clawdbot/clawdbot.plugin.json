{
  "id": "repoql",
  "name": "RepoQL",
  "description": "Queryable knowledge graph for repositories. X-ray vision for code understanding.",
  "version": "1.0.0",
  "kind": "tool",
  "repository": "https://github.com/stueeey/RepoQL.Public",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "exePath": {
        "type": "string",
        "description": "Path to repoql executable. Auto-detected from PATH if not set."
      },
      "healthCheckIntervalMs": {
        "type": "number",
        "description": "Health check interval in milliseconds.",
        "default": 60000
      },
      "maxRestartAttempts": {
        "type": "number",
        "description": "Maximum restart attempts before giving up.",
        "default": 3
      },
      "defaultTimeoutMs": {
        "type": "number",
        "description": "Default timeout for tool calls in milliseconds.",
        "default": 60000
      },
      "workspaceAsRepo": {
        "type": "boolean",
        "description": "Use the agent workspace as the repository root.",
        "default": true
      }
    }
  },
  "uiHints": {
    "exePath": {
      "label": "RepoQL Executable Path",
      "placeholder": "Leave blank to auto-detect from PATH"
    },
    "healthCheckIntervalMs": {
      "label": "Health Check Interval (ms)",
      "help": "How often to check if RepoQL instances are healthy."
    },
    "maxRestartAttempts": {
      "label": "Max Restart Attempts",
      "help": "Number of times to attempt restarting a crashed instance."
    },
    "defaultTimeoutMs": {
      "label": "Default Timeout (ms)",
      "help": "Default timeout for tool calls. Individual tools may override."
    },
    "workspaceAsRepo": {
      "label": "Use Workspace as Repository",
      "help": "Index the agent's workspace directory."
    }
  },
  "skills": [
    "skills/repoql",
    "skills/repoql-sql",
    "skills/repoql-search"
  ]
}
